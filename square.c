#include <stdio.h>
// write a program to to print the square of a number :)
// algorithm is a sequence or set of instructions in which a task is performed
int main()

{
    // start the program

    int x, square; // declaring a variable


    printf("enter the value of x:- ");
    scanf("%d", &x); // take the value from user
      //infix expression 
    square = x*x;

    // calculate the square

    printf("the value of x square is :-   %d", square); // print the value of square

    return 0; // end the program
}
